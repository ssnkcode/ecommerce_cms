# ============================================================
#  Configuración del frontend (CMS + catálogo)
#  Copiá este archivo a `.env` solo si querés sobreescribir valores.
# ============================================================

# NO setear VITE_API_URL.
# El front público queda 100% estático y consume catalog/data.json.
# Para el desarrollo local, utils/api.js deduce http://localhost:3001 por
# defecto (el backend local corre en ese puerto).


