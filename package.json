{
  "name": "cms",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "prebuild": "node ./db/export-local-json.mjs",
    "build": "npm --prefix cms run build"
  },
  "dependencies": {
    "axios": "^1.20.0"
  }
}
